name: Generate md5 sums
on: [push]
jobs:
  Publish:
    runs-on: macos-12
    steps:
      - name: 'Checkout Repo'
        uses: actions/checkout@v2

      - name: Get the kernel upgrade script
        id: publish
        run: |
          find /usr/share/firmware -name "*.ptb"
        #  system_profiler SPHardwareDataType | grep "Model Identifier"
        #  bash ./firmware.sh
          
