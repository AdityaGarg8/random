name: Generate md5 sums
on: [push]
jobs:
  Retrieve-Firmware:
    runs-on: ubuntu-24.04
    steps:
      - name: Free up disk space for the CI
        uses: AdityaGarg8/remove-unwanted-software@v3
        with:
          remove-android: 'false'
          remove-dotnet: 'false'
          remove-haskell: 'false'
          remove-codeql: 'false'
          remove-docker-images: 'false'

      - name: 'Checkout Repo'
        uses: actions/checkout@v4

      - name: Get kconfig
        run: |
          #wget -m -np -nH --cut-dirs=3 -e robots=off --reject "index.html*" -P ${{ github.workspace }}/repo https://pkg-repo.blendos.co
          git clone --depth 1 https://github.com/torvalds/linux
          git clone https://github.com/t2linux/linux-t2-patches.git
          cd linux-t2-patches
          cp ./linux-t2-patches/300* ./linux
          cd linux
          git am 3001*
          git am 3002*
          git am 3003*
          git config --global user.name "example example"
          git config --global user.email "example@example.com"
      - name: Upload package artifact
        uses: actions/upload-artifact@v4
        with:
          name: blendos
          #path: /mnt/casper/*.manifest
          path: ${{ github.workspace }}/linux/drivers/hwmon/applesmc.c
